# Ignore all .log files
*.log

# Ignore all files in the 'build/' directory
build/

# Ignore the 'node_modules/' directory and its contents
node_modules/

# Ignore specific files by name
.DS_Store
Thumbs.db

# Ignore all files with a .tmp extension, regardless of directory
**/*.tmp

# Ignore all files named 'credentials.txt' anywhere in the repository
credentials.txt

# Ignore everything in the 'logs/' directory, but not the directory itself
logs/*

# Exclude a specific file from a previously ignored pattern
# (e.g., ignore all .md files except README.md)
*.md
!README.md

# Ignore all files and directories starting with 'temp'
temp*